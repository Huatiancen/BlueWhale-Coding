# BlueWhale 演示指令

在 BlueWhale 首页选择当前文件夹，然后依次尝试：

1. `阅读这个项目，找出测试失败的根因并修复。不要修改测试。`
2. `运行项目测试，若失败则继续修复，最后说明修改了哪些文件以及验证结果。`
3. `撤销 ledger.py 的本轮修改。`

本项目故意包含两个缺陷：退款被过滤，以及金额通过浮点数转换为分导致精度不稳定。初始验证命令如下，预期应失败：

```bash
python -m unittest discover -s tests -v
```

修复后同一命令应通过，可用于演示独立验证、Diff、工作过程和按文件撤销。

